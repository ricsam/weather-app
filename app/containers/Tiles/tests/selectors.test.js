// import { fromJS } from 'immutable';
// import { makeSelectTilesDomain } from '../selectors';

// const selector = makeSelectTilesDomain();

describe('makeSelectTilesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
