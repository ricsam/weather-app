// import { fromJS } from 'immutable';
// import { makeSelectTileDomain } from '../selectors';

// const selector = makeSelectTileDomain();

describe('makeSelectTileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
